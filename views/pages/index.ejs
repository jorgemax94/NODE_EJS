<!DOCTYPE html>

    <%- include('../partials/head'); %>

    <body class="container">
    <%- include('../partials/header'); %>
        <main>
            <div class="jumbotron">
                <h1>Node com EJS</h1>
                <p>Aprendendo a trabalhar com arquivos EJS</p>
                <p><%= subtitle %></p>
            </div>
            <ul>
                <% qualidades.forEach((q)=>{%>
                        <li>
                            <strong><%= q.title %> </strong> <%= q.message.substring(1) %>
                        </li>
                <% }) %>
            </ul>
        </main>
        <%- include('../partials/footer'); %>
    </body>
</html>